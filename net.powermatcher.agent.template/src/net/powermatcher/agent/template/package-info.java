/**
 * Example project for developing agent that use features beyond the core.
 * <p>
 * The <code>net.powermatcher.agent.template</code> and <code>net.powermatcher.agent.template.component</code> projects extend the
 * core agent template projects, <code>net.powermatcher.core.agent.template</code> and <code>net.powermatcher.core.agent.template.component</code>,
 * and projects serve as starting point for developing a new PowerMatcher Agent that use extension libraries. The template project implements an example
 * agent, that publishes telemetry data.
 * <ul>
 * <li><code><b>ExampleAgent3</b></code><br>An extension of core <code>ExampleAgent1</code> that also publishes an update count as telemetry measurement data.</li>
 * </ul> 
 *
 * @since 0.7
 */
package net.powermatcher.agent.template;